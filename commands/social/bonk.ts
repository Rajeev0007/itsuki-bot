import { createSocialCommand } from '../../utils/SocialCommandFactory';

export default createSocialCommand({
  action:    'bonk',
  emoji:     '🔨',
  pastTense: 'bonked',
  plural:    'bonks',
  category:  'social',
});
